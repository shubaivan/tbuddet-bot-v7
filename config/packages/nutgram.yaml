nutgram:
  token: '%env(string:TELEGRAM_TOKEN)%'
  routes: true
  config:
    # Photo uploads from disk regularly exceed the 5s default when the bot
    # is sending bytes for the first time. 60s gives Telegram room to ack
    # without forcing a sendMessage fallback / double-message.
    clientTimeout: 60
